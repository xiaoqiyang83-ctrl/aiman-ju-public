# AIManJu 分镜拆分逻辑改造指令

## 问题概述

当前 `generateShotsForScene` 函数（routes/scripts.js 约第646行）存在严重问题：
- **按字数均分镜头**：每150字一个镜头，不懂剧情节奏
- **景别固定顺序**：第1个永远全景，第2个永远远景……
- **内容按字数切割**：可能在句子中间断开
- **画面描述是原文截取**：没有转化为视觉语言（AI无法根据原文生成画面）
- **结果**：拆出来的镜头内容是场景标题或台词文字，不是视觉画面描述

## 改造目标

将 `generateShotsForScene` 函数完全重写，实现行业标准的6步分镜逻辑。

---

## 改造1：内容类型识别

在拆分镜头之前，先判断场景的内容类型。在 `generateShotsForScene` 函数开头新增：

```javascript
function detectSceneType(text) {
  // 识别规则（按优先级）：
  // 1. 对话场景：文本中包含大量引号对（"..."或「...」）或"说道""喊道"等对话标记
  // 2. 动作/冲突场景：包含打斗、奔跑、碰撞等动词密集区
  // 3. 情绪特写：包含哭泣、笑、愤怒等情绪词
  // 4. 环境交代：以环境描写为主，人物动作为辅
  // 5. 过渡/行走：简单的空间移动
  // 6. 关键线索：需要特写放大的物品或细节

  const dialoguePattern = /[""「」""].*?[""「」""]/g;
  const actionKeywords = ['打', '跑', '冲', '撞', '踢', '跳', '抓', '摔', '挥', '劈', '闪', '扑', '逃', '追'];
  const emotionKeywords = ['哭', '泪', '笑', '怒', '颤抖', '崩溃', '绝望', '惊喜', '感动', '委屈', '悲伤'];
  const transitionKeywords = ['走', '来到', '进入', '离开', '穿过', '走向'];

  const dialogueMatches = text.match(dialoguePattern) || [];
  const hasAction = actionKeywords.some(k => text.includes(k));
  const hasEmotion = emotionKeywords.some(k => text.includes(k));
  const hasTransition = transitionKeywords.some(k => text.includes(k));

  // 判断逻辑
  if (dialogueMatches.length >= 2) return 'dialogue';      // 对话场景
  if (hasAction && hasEmotion) return 'action_emotion';     // 动作+情绪
  if (hasAction) return 'action';                           // 动作场景
  if (hasEmotion) return 'emotion';                         // 情绪特写
  if (hasTransition && dialogueMatches.length <= 1) return 'transition'; // 过渡
  return 'environment';                                      // 默认环境交代
}
```

## 改造2：镜头模板系统

不同内容类型使用不同的镜头组合模板：

```javascript
const SHOT_TEMPLATES = {
  dialogue: {
    // 对话场景：全景(交代) → 中景(A说话) → 近景(B反应) → 近景(A继续) → 中景(双人)
    shots: ['全景', '中景', '近景', '近景', '中景'],
    movements: ['static', 'static', 'push_in', 'push_in', 'static'],
    durations: [5, 4, 3, 3, 4]
  },
  action: {
    // 动作场景：全景→中景→近景→特写→全景（高频切换）
    shots: ['全景', '中景', '近景', '特写', '全景'],
    movements: ['static', 'tracking', 'tracking', 'push_in', 'pull_back'],
    durations: [4, 3, 2, 2, 4]
  },
  action_emotion: {
    // 动作+情绪：全景→中景→近景→特写→大特写→全景
    shots: ['全景', '中景', '近景', '特写', '大特写', '全景'],
    movements: ['static', 'tracking', 'push_in', 'push_in', 'push_in', 'pull_back'],
    durations: [4, 3, 3, 2, 3, 4]
  },
  emotion: {
    // 情绪特写：中景→近景→特写→大特写（递进聚焦）
    shots: ['中景', '近景', '特写', '大特写'],
    movements: ['static', 'push_in', 'push_in', 'push_in'],
    durations: [4, 4, 4, 5]
  },
  environment: {
    // 环境交代：远景→全景→中景（缓慢推进）
    shots: ['远景', '全景', '中景'],
    movements: ['static', 'pan', 'push_in'],
    durations: [5, 5, 4]
  },
  transition: {
    // 过渡/行走：全景→中景(跟拍)
    shots: ['全景', '中景'],
    movements: ['static', 'tracking'],
    durations: [4, 4]
  }
};
```

## 改造3：智能内容拆分（替代字数均分）

根据内容类型，将场景文本智能拆分到每个镜头：

```javascript
function splitContentByType(text, sceneType, shotCount) {
  // 1. 对话场景：按对话轮次拆分
  if (sceneType === 'dialogue') {
    return splitDialogueContent(text, shotCount);
  }

  // 2. 其他场景：按语义段落拆分
  // 先按句号/感叹号/问号断句
  const sentences = text.split(/(?<=[。！？\.\!\?])/g).filter(s => s.trim());
  
  if (sentences.length <= shotCount) {
    // 句子数少于镜头数，每个句子一个镜头，不足的重复最后一句
    const result = [];
    for (let i = 0; i < shotCount; i++) {
      result.push(sentences[Math.min(i, sentences.length - 1)]);
    }
    return result;
  }

  // 句子数多于镜头数，按语义段分组
  const perShot = Math.ceil(sentences.length / shotCount);
  const result = [];
  for (let i = 0; i < shotCount; i++) {
    const start = i * perShot;
    const end = Math.min(start + perShot, sentences.length);
    result.push(sentences.slice(start, end).join(''));
  }
  return result;
}

function splitDialogueContent(text, shotCount) {
  // 对话场景特殊处理：
  // 提取对话轮次，每2-3轮对话对应一个镜头
  const dialoguePattern = /([^""「」""]*?)([""「」""].*?[""「」""])/g;
  const parts = [];
  let match;
  while ((match = dialoguePattern.exec(text)) !== null) {
    if (match[1].trim()) parts.push({ type: 'narration', text: match[1].trim() });
    parts.push({ type: 'dialogue', text: match[2] });
  }
  
  // 如果正则没匹配到，fallback到普通拆分
  if (parts.length === 0) {
    return splitContentByType(text, 'other', shotCount);
  }

  // 将parts均匀分配到镜头
  const perShot = Math.max(1, Math.ceil(parts.length / shotCount));
  const result = [];
  for (let i = 0; i < shotCount; i++) {
    const start = i * perShot;
    const end = Math.min(start + perShot, parts.length);
    const shotParts = parts.slice(start, end);
    result.push(shotParts.map(p => p.text).join(''));
  }
  return result;
}
```

## 改造4：画面描述转化为视觉语言（最关键！）

**这是最重要的改造！当前画面描述就是原文截取，AI根本无法根据台词文字生成画面。**

每个镜头的 `visual_prompt`（画面描述）必须按这个模板生成：

```
[景别]，[主体]，[动作/表情]，[场景环境]，[光影氛围]
```

```javascript
function generateVisualPrompt(shotType, contentPart, sceneContext, movement) {
  // sceneContext 包含：地点、时间、角色列表等
  
  // 1. 从内容中提取视觉元素
  const characters = extractCharacters(contentPart);    // 提及的角色
  const actions = extractActions(contentPart);           // 动作词
  const emotions = extractEmotions(contentPart);         // 情绪词
  const objects = extractKeyObjects(contentPart);        // 关键物品

  // 2. 根据景别选择描述重点
  let subject = '';
  let detail = '';
  
  switch(shotType) {
    case '远景':
      subject = sceneContext.location || '场景全景';
      detail = `展现${subject}全貌，${sceneContext.timeOfDay || '自然光'}`;
      break;
    case '全景':
      subject = characters.length > 0 ? characters.join('和') : sceneContext.location;
      detail = `${subject}在${sceneContext.location}中，${actions[0] || '站立'}`;
      break;
    case '中景':
      subject = characters[0] || '角色';
      detail = `${subject}${actions[0] || '的半身画面'}，背景${sceneContext.location}`;
      break;
    case '近景':
      subject = characters[0] || '角色';
      detail = `${subject}面部特写，${emotions[0] || '表情专注'}，背景虚化`;
      break;
    case '特写':
      if (objects.length > 0) {
        detail = `${objects[0]}的特写镜头，细节清晰`;
      } else {
        detail = `${characters[0] || '角色'}的${emotions[0] ? emotions[0] + '表情' : '手部/眼神'}特写`;
      }
      break;
    case '大特写':
      if (objects.length > 0) {
        detail = `${objects[0]}极近距离特写，纹理细节清晰可见`;
      } else {
        detail = `${characters[0] || '角色'}眼神极近距离特写，${emotions[0] || '情绪'}饱满`;
      }
      break;
  }

  // 3. 组装运镜描述
  const movementDesc = {
    'static': '固定镜头',
    'push_in': '缓慢推进',
    'pull_back': '缓慢拉远',
    'tracking': '跟拍',
    'pan': '缓慢摇镜'
  };

  return `${shotType}，${detail}，${movementDesc[movement] || '固定镜头'}，${sceneContext.lighting || '自然光'}`;
}
```

辅助提取函数（简单版，不需要AI，用关键词匹配即可）：

```javascript
function extractCharacters(text) {
  // 从文本中提取角色名
  // 方法：查找"说道""看着""走向"等动词前面的2-4字名词
  const pattern = /([\u4e00-\u9fa5]{2,4})(?:说|道|看|望|走|跑|冲|喊|叫|握|抓|拉|推|笑|哭|怒|叹)/g;
  const matches = [];
  let match;
  while ((match = pattern.exec(text)) !== null) {
    const name = match[1];
    if (!['然后', '接着', '突然', '这时', '于是', '只见', '此时'].includes(name)) {
      matches.push(name);
    }
  }
  return [...new Set(matches)].slice(0, 3);
}

function extractActions(text) {
  const actionVerbs = ['走', '跑', '坐', '站', '看', '望', '抓', '握', '推', '拉', '举', '放', '跪', '蹲', '跳', '挥', '指', '拍', '抱', '扶'];
  return actionVerbs.filter(v => text.includes(v));
}

function extractEmotions(text) {
  const emotionMap = {
    '哭': '流泪', '泪': '含泪', '笑': '微笑', '怒': '愤怒', '惊': '惊讶',
    '怕': '恐惧', '抖': '颤抖', '叹': '叹气', '默': '沉默', '僵': '僵硬'
  };
  return Object.entries(emotionMap).filter(([k]) => text.includes(k)).map(([, v]) => v);
}

function extractKeyObjects(text) {
  // 提取可能的物品关键词（简单版）
  const objectPattern = /([\u4e00-\u9fa5]{2,4})(?:上的|手中的|面前的|桌上的|怀里的)/g;
  const matches = [];
  let match;
  while ((match = objectPattern.exec(text)) !== null) {
    matches.push(match[1]);
  }
  return matches.slice(0, 2);
}
```

## 改造5：重写 generateShotsForScene 主函数

将以上所有改造整合，重写主函数：

```javascript
async function generateShotsForScene(scene, scriptContent) {
  // 第1步：识别场景类型
  const sceneText = scene.rawContent || scene.description || '';
  const sceneType = detectSceneType(sceneText);

  // 第2步：获取镜头模板
  const template = SHOT_TEMPLATES[sceneType] || SHOT_TEMPLATES.environment;
  let shotCount = template.shots.length;

  // 第3步：如果文本很长，按比例增加镜头数
  if (sceneText.length > 300) {
    const extraShots = Math.floor((sceneText.length - 300) / 200);
    shotCount = Math.min(shotCount + extraShots, 10); // 最多10个镜头
  }

  // 第4步：智能拆分内容
  const contentParts = splitContentByType(sceneText, sceneType, shotCount);

  // 第5步：构建场景上下文
  const sceneContext = {
    location: scene.location || scene.title || '',
    timeOfDay: scene.time_of_day || '',
    characters: extractCharacters(sceneText),
    lighting: ''
  };
  // 根据时间推断光影
  if (sceneContext.timeOfDay.includes('夜') || sceneContext.timeOfDay.includes('晚')) {
    sceneContext.lighting = '暗调光线，月光/灯光';
  } else if (sceneContext.timeOfDay.includes('日') || sceneContext.timeOfDay.includes('早')) {
    sceneContext.lighting = '明亮自然光';
  } else {
    sceneContext.lighting = '自然光';
  }

  // 第6步：生成镜头
  const shots = [];
  for (let i = 0; i < shotCount; i++) {
    const templateIndex = i % template.shots.length;
    const shotType = template.shots[templateIndex];
    const movement = template.movements[templateIndex];
    const duration = template.durations[templateIndex];
    const contentPart = contentParts[i] || contentParts[contentParts.length - 1];

    // 生成视觉语言画面描述（关键改造！）
    const visualPrompt = generateVisualPrompt(shotType, contentPart, sceneContext, movement);

    shots.push({
      scene_id: scene.id,
      shot_number: i + 1,
      shot_type: shotType,
      duration: duration,
      description: visualPrompt,           // 视觉语言画面描述（给AI生成用）
      original_text: contentPart,          // 保留原文（给配音/字幕用）
      camera_movement: movement,           // 运镜方式
      visual_prompt: visualPrompt,         // AI图片/视频生成提示词
      status: 'pending'
    });
  }

  return shots;
}
```

---

## 数据库字段说明

确认 shots 表是否有以下字段，如果没有需要新增：

| 字段 | 类型 | 说明 |
|------|------|------|
| visual_prompt | TEXT | AI生成用视觉描述 |
| original_text | TEXT | 原文内容（配音/字幕用） |
| camera_movement | VARCHAR(50) | 运镜方式 |

如果 shots 表当前只有 `description` 字段，可以：
- `description` 存视觉语言画面描述（替代原来的原文截取）
- 新增 `original_text` 存原文
- 新增 `camera_movement` 存运镜方式

---

## 改造优先级

1. **P0（必须做）**：重写 generateShotsForScene，实现内容类型识别 + 镜头模板 + 视觉语言转化
2. **P1（重要）**：新增 visual_prompt、original_text、camera_movement 字段
3. **P2（优化）**：前端分镜卡片显示优化，区分"画面描述"和"原文"

---

## 验收标准

改造完成后，用以下剧本片段测试：

```
日内 工厂农场-温室

队员甲盯着面前的番茄，惊愕地说："番茄不是早就灭绝了吗？"

队员乙小心翼翼地触碰番茄表面，手指微微颤抖："它...它是真的。"

队长面色凝重，压低声音："都别碰，先记录下来。"
```

**改造前**（当前结果）：
- 镜头1：全景，画面="1-10集"（无意义）
- 镜头2：中景，画面="第2集"（标题）
- 镜头3：近景，画面=台词原文（不是视觉描述）

**改造后**（期望结果）：
- 镜头1：全景，画面="温室内部全景，三人在番茄植株前，明亮温室灯光"
- 镜头2：中景，画面="队员甲半身画面，盯着面前的番茄，惊愕表情，背景虚化的温室植物"
- 镜头3：近景，画面="队员乙面部特写，小心翼翼的表情，手指轻触番茄表面微微颤抖，背景虚化"
- 镜头4：近景，画面="队长面部特写，面色凝重，压低声音说话，侧光"
- 镜头5：中景，画面="三人中景，温室环境，紧张氛围"

**关键词**：识别为对话场景 → 使用对话模板 → 景别按[全景→中景→近景→近景→中景]分配 → 画面描述转化为视觉语言
